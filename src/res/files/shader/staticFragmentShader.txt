#version 130

uniform sampler2D tex;

in vec2 fragTex;
in float brightness;

out vec4 finalColor;

void main() {
    finalColor = texture2D(tex, fragTex);
    finalColor = vec4(finalColor.rgb * (brightness*0.5 + 0.5), finalColor.a);
}